const express = require('express'); const cors = require('cors'); const app = express(); app.use(cors()); app.get('/api/search', (req, res) => { res.json({ results: ['Result 1 for ' + req.query.q] }); }); app.listen(5000, () => console.log('Backend running on port 5000'));
